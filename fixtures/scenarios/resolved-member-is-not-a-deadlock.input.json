{
	"description": "Three tickets whose edges close a loop, one of them already closed. The edges alone would call this a deadlock; it is not one, because a closed blocker is a met dependency — so the ticket waiting on it is recommendable and the loop is broken where it was resolved.",
	"truncated": false,
	"openOnly": false,
	"filter": { "include": [], "exclude": ["wayfinder:*"] },
	"tickets": [
		{ "ref": "gh:example/repo#1", "title": "Migrate the store", "state": "open", "blockers": ["gh:example/repo#2"], "labels": [] },
		{ "ref": "gh:example/repo#2", "title": "Cut over the reads", "state": "closed", "blockers": ["gh:example/repo#3"], "labels": [] },
		{ "ref": "gh:example/repo#3", "title": "Delete the old store", "state": "open", "blockers": ["gh:example/repo#1"], "labels": [] }
	]
}
