{
	"description": "A ticket set read as open tickets only, which is what the GitHub read asks for. Every other scenario states its own closed tickets and so reports a count of them; here nothing was asked, and the counts line has to say that rather than report the zero it would otherwise be — the collapse CONTEXT.md forbids for blocking, in the one other place a zero can mean \"never asked\".",
	"truncated": false,
	"openOnly": true,
	"filter": { "include": [], "exclude": ["wayfinder:*"] },
	"tickets": [
		{ "ref": "gh:example/repo#1", "title": "Land the parser", "state": "open", "blockers": [], "labels": ["P1"] },
		{ "ref": "gh:example/repo#2", "title": "Wire the parser in", "state": "open", "blockers": ["gh:example/repo#1"], "labels": [] },
		{ "ref": "gh:example/repo#3", "title": "Update the changelog", "state": "open", "claim": { "by": "octocat" }, "blockers": [], "labels": [] }
	]
}
