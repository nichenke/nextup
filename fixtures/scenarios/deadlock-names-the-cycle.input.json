{
	"description": "The same migration with its first two tickets each waiting on the other, and a third waiting behind them. Nothing is recommendable and nothing ever will be, so the answer names the two tickets in the loop rather than reporting the same empty candidate set a merely blocked backlog gets. The third is blocked by the cycle and is not named in it: it carries no edge a reader could follow back.",
	"truncated": false,
	"openOnly": false,
	"filter": { "include": [], "exclude": ["wayfinder:*"] },
	"tickets": [
		{ "ref": "gh:example/repo#1", "title": "Migrate the store", "state": "open", "blockers": ["gh:example/repo#2"], "labels": [] },
		{ "ref": "gh:example/repo#2", "title": "Cut over the reads", "state": "open", "blockers": ["gh:example/repo#1"], "labels": [] },
		{ "ref": "gh:example/repo#3", "title": "Delete the old store", "state": "open", "blockers": ["gh:example/repo#1"], "labels": [] }
	]
}
