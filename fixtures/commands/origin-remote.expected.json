{
	"description": "The remote a repository-scoped short form is resolved against, read from the repository's own config and what it includes, so no global one can answer for it.",
	"input": {
		"directory": "/repo"
	},
	"argv": [
		"git",
		"-C",
		"/repo",
		"config",
		"--show-scope",
		"--includes",
		"--get-all",
		"remote.origin.url"
	]
}
