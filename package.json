{
  "name": "nextup",
  "version": "0.1.0",
  "private": true,
  "description": "Unblocked Opportunist - picks the best unclaimed, unblocked ticket and starts work on it",
  "type": "module",
  "bin": {
    "nextup": "bin/nextup.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check:identifiers": "bash scripts/check-identifiers.sh",
    "check:live": "bun scripts/reconstruct.ts",
    "provision:test-tree": "bun scripts/provision-test-tree.ts",
    "capture:github": "bun scripts/capture-github-recordings.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
