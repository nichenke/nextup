# 14 — Confirm v3 live and retire what it supersedes

**What to build:** The GitLab adapter is confirmed working against the real GitLab ticket map, and
anything it supersedes is removed.

**Blocked by:** 13

**Status:** ready-for-agent

- [ ] Exercised live against the real GitLab-hosted map, end to end including claim, worktree, and launch
- [ ] Any bespoke GitLab ticket handling that now duplicates the tool is deleted; if there is none, that is recorded explicitly rather than left ambiguous
- [ ] That repository's documentation points at `nextup`
